'use strict';

/**
 * active-stage-list service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::active-stage-list.active-stage-list');
