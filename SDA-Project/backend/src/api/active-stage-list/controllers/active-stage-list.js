'use strict';

/**
 * active-stage-list controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::active-stage-list.active-stage-list');
